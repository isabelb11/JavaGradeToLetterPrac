package grade;

public class ConditionalOperatorRecap {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		boolean x = true ? false: true;
		System.out.println(x);
		int a = 10; 
		int b = 5; 
		boolean y = a > b ? true : false;
		
//	     conditions ? true statement : false statement
        // if the conditions return true, we will be using the 
        // true statement which is right after the ? mark
        // if the conditions return false, we will be using the 
        // false statement, which is right after the : mark

	int num  = 15;
	if (num % 3 == 0 && num % 5 == 0) {
		System.out.println("TEK SCHOOL");
	}else if (num % 3 == 0) {
		
	}

		
	}

}
